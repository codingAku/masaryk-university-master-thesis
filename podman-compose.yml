version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: dockerfile
    network_mode: slirp4netns:allow_host_loopback=true
    ports:
      - "8501:8501"      # Streamlit app port
      - "11434:11434"    # Ollama API port
    volumes:
      - .:/app
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@10.0.2.2:5432/mydb
    depends_on:
      - db
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  db:
    image: postgres:13
    network_mode: slirp4netns:allow_host_loopback=true
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: mydb
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  pgdata: